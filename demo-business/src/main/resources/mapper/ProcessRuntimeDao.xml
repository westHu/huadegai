<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hup.dao.ProcessRuntimeDao">

    <!--
        1.属性名,表名等用小写,select / from之类关键字用大写。
        2.<select>返回值尽量使用resultType，当实体属性名称与表中列名无法匹配时候，使用resultMap进行映射。
        3.不要用parameterType属性(多余)。
        4.一般情况下用#{},不要用${}。
        5.对做了分表分库后的数据集做查询操作时，不支持Join, 嵌套查询，order by, group by等操作。
        6.对SQL进行排版。
        7.建立索引，在数据量大的数据表中做查询的时候，对关键字段建立索引。（具体是否要加索引，请与数据组同事讨论）
          WHERE t.username = #{userName}
    -->
   <select id="findAllRuntime" resultType="com.hup.entity.ProcessRuntime">
       SELECT * FROM flow_process_runtime GROUP BY name
   </select>

    <insert id="insertProcessRuntime" useGeneratedKeys="true" keyProperty="runtime.id">
        INSERT INTO flow_process_runtime
        (name, name_desc, step, step_desc, code,  members, groups, remark, receipted, executed, comment, create_date)
        VALUES
        (#{runtime.name}, #{runtime.nameDesc}, #{runtime.step}, #{runtime.stepDesc}, #{runtime.code}, #{runtime.members},
         #{runtime.groups}, #{runtime.remark}, #{runtime.receipted}, #{runtime.executed}, #{runtime.comment}, NOW())
    </insert>

    <select id="findByCode" resultType="com.hup.entity.ProcessRuntime">
        SELECT * FROM flow_process_runtime t WHERE t.code = #{code}
    </select>


    <select id="findByCodeAndExecuted" resultType="com.hup.entity.ProcessRuntime">
        SELECT * FROM flow_process_runtime t WHERE
        t.code = #{code}
        <if test="executed != null and executed == true">
            AND t.executed is not null
        </if>
        <if test="executed != null and executed == false">
            AND t.executed is null
        </if>
    </select>

    <select id="findByCodeAndStep" resultType="com.hup.entity.ProcessRuntime">
         SELECT * FROM flow_process_runtime t WHERE
                t.code = #{code} AND t.step = #{step}
    </select>

    <update id="updateRuntimeService">
        UPDATE flow_process_runtime SET
        receipted = #{receipted},
        executed = #{executed},
        comment = #{comment}
        WHERE code =#{code} AND step = #{step}
    </update>


</mapper>