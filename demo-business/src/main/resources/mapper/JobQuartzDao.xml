<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hup.dao.JobQuartzDao">

    <!--
        1.属性名,表名等用小写,select / from之类关键字用大写。
        2.<select>返回值尽量使用resultType，当实体属性名称与表中列名无法匹配时候，使用resultMap进行映射。
        3.不要用parameterType属性(多余)。
        4.一般情况下用#{},不要用${}。
        5.对做了分表分库后的数据集做查询操作时，不支持Join, 嵌套查询，order by, group by等操作。
        6.对SQL进行排版。
        7.建立索引，在数据量大的数据表中做查询的时候，对关键字段建立索引。（具体是否要加索引，请与数据组同事讨论）
          WHERE t.username = #{userName}
    -->
    <select id="queryJobQuartzList" resultType="com.hup.entity.JobQuartz">
        SELECT * FROM job_quartz t WHERE  1=1
        <if test="jobQuartz.jobName != null and jobQuartz.jobName != ''">
            AND t.job_name = #{jobQuartz.jobName}
        </if>
    </select>

    <select id="getJobQuartzCount" resultType="java.lang.Integer">
        SELECT COUNT(1) FROM job_quartz t
        <if test="jobQuartz.jobName != null and jobQuartz.jobName != ''">
            AND t.job_name = #{jobQuartz.jobName}
        </if>
    </select>

    <delete id="deleteJobQuartzByType">
        DELETE FROM job_quartz WHERE job_type = #{jobType}
    </delete>

    <insert id="insertJobQuartz">
        INSERT INTO  job_quartz
        (job_name, job_group_name, trigger_name, trigger_group_name, time, job_creater, job_type, status, create_date)
        VALUES
        (#{jobQuartz.jobName},#{jobQuartz.jobGroupName},#{jobQuartz.triggerName},#{jobQuartz.triggerGroupName},
        #{jobQuartz.time},#{jobQuartz.jobCreater},#{jobQuartz.jobType}, #{jobQuartz.status}, NOW())
    </insert>

    <select id="findOne" resultType="com.hup.entity.JobQuartz">
         SELECT * FROM job_quartz t WHERE  id = #{id}
    </select>

    <update id="updateStatus">
        UPDATE job_quartz SET
        status = #{jobQuartz.status}
        WHERE id = #{jobQuartz.id}
    </update>

    <select id="findOneByJobName" resultType="com.hup.entity.JobQuartz">
        SELECT * FROM job_quartz t WHERE  job_name = #{jobName}
    </select>

    <delete id="deleteJobQuartz">
        DELETE FROM job_quartz  WHERE  id = #{id}
    </delete>


    <select id="getQuartzJobByStatus" resultType="com.hup.entity.JobQuartz">
        SELECT * FROM job_quartz t WHERE  status = #{status}
    </select>

</mapper>