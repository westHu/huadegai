<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hup.dao.PatrolPlanDao">

    <!--
        1.属性名,表名等用小写,select / from之类关键字用大写。
        2.<select>返回值尽量使用resultType，当实体属性名称与表中列名无法匹配时候，使用resultMap进行映射。
        3.不要用parameterType属性(多余)。
        4.一般情况下用#{},不要用${}。
        5.对做了分表分库后的数据集做查询操作时，不支持Join, 嵌套查询，order by, group by等操作。
        6.对SQL进行排版。
        7.建立索引，在数据量大的数据表中做查询的时候，对关键字段建立索引。（具体是否要加索引，请与数据组同事讨论）
          WHERE t.username = #{userName}
    -->
    <sql id="table_columns">

    </sql>

    <select id="getAllPatrolPlan" resultType="com.hup.entity.PatrolPlan">
        SELECT * FROM pps_patrol_plan t WHERE t.status = 1
    </select>

    <select id="queryPatrolPlanList" resultType="com.hup.entity.PatrolPlan">
        SELECT * FROM pps_patrol_plan
    </select>

    <select id="getPatrolPlanCount" resultType="java.lang.Integer">
        SELECT COUNT(1) FROM pps_patrol_plan
    </select>

    <insert id="insertPlan" useGeneratedKeys="true" keyProperty="patrolPlan.id">
        INSERT INTO pps_patrol_plan
        (plan_name, plan_desc, plan_creater, plan_begin, plan_end, plan_per_hour, point_ids, status, related_quartz_job, create_date)
        VALUES
        (#{patrolPlan.planName}, #{patrolPlan.planDesc}, #{patrolPlan.planCreater}, #{patrolPlan.planBegin},
        #{patrolPlan.planEnd}, #{patrolPlan.planPerHour}, #{patrolPlan.pointIds}, 0, 0,  NOW())
    </insert>

    <delete id="deletePlan">
        DELETE FROM pps_patrol_plan WHERE id = #{id}
    </delete>

    <select id="getPlan" resultType="com.hup.entity.PatrolPlan">
        SELECT * FROM pps_patrol_plan WHERE id = #{id}
    </select>

    <update id="updatePlan">
        UPDATE pps_patrol_plan SET
        plan_name = #{patrolPlan.planName},
        plan_desc = #{patrolPlan.planDesc},
        plan_creater = #{patrolPlan.planCreater},
        plan_begin = #{patrolPlan.planBegin},
        plan_end = #{patrolPlan.planEnd},
        plan_per_hour = #{patrolPlan.planPerHour},
        point_ids = #{patrolPlan.pointIds},
        WHERE id = #{patrolPlan.id}
    </update>

    <delete id="updatePlanRelatedJob">
         UPDATE pps_patrol_plan SET
        related_quartz_job = #{patrolPlan.relatedQuartzJob}
        WHERE id = #{patrolPlan.id}
    </delete>

</mapper>